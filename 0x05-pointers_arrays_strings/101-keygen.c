#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Description: keygen
 *
 * Return: password
 */

int main(void)
{
        int i, n;

        /*time_t t;*/
        srand(time(NULL));

        for( i = 0 ; i < 100 ; i++)
        {
		n = rand()
                printf("%d\n", n);
        }

        return (0);
}
