#include <string.h>

/**
 * _strlen - takes in the variable in question
 * @s: the variable to be measured
 *
 * Description: takes in a string and measure it's length
 *
 * Return: string length
 */

int _strlen(char *s)
{
	int n = strlen(s);

	return (n);
}
